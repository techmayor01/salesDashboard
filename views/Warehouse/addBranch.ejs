<!DOCTYPE html>
<html lang="en">
<%- include ('../partials/head') %>
<body>
   <div class="relative flex flex-col min-h-screen bg-slate-200">
     <%- include ('../partials/header') %>

    <div class="flex flex-row">
        <div class="w-[220px] p-4"><%- include ('../partials/sidebar') %></div>
        <main
          class="cal relative top-16 p-4 overflow-auto"
          style="width: calc(100% - 220px)">
            <div class="flex flex-col justify-between items-start">
                <h1 class="text-2xl font-semibold mb-3">Add Branch</h1>
               
            </div>

           <!-- Form Fields -->
        <form action="/addBranch" method="post" class="bg-white rounded-xl shadow p-6 space-y-6 border border-gray-100 hover:bg-gray-50">
            <!-- Supplier Name -->
             <div class="flex flex-col justify-between items-start">
                <h1 class="text-2xl font-semibold text-gray-600">Add Branch</h1> 
                <p class="text-xs text-gray-400 py-2">use this form to add Branch to database..</p>
            </div>
            <div class="grid grid-cols-1 sm:grid-cols-3 gap-4 items-center">
                <label class="text-gray-700 font-medium">Branch <span class="text-red-500">*</span></label> 
                <input type="text" name="branch_name" class="sm:col-span-2 px-4 py-2 border rounded-md w-full focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Branch">
            </div>

            
            <!-- Submit Button -->
            <div class="flex justify-center items-center ">
                <button class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-2 rounded-md text-sm font-semibold transition duration-200">Add Branch</button>
            </div>
           
        </form>
         <%- include ('../partials/footer') %>
        </main>
    </div>

   </div>
</body>
</html>